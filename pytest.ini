[pytest]
testpaths = tests
norecursedirs = utils/searxng utils/daily_stock_analysis utils/news_public_opinion .venv .web
filterwarnings =
    ignore::RuntimeWarning
    ignore::DeprecationWarning
markers =
    integration: tests that access network or external services
    benchmark: performance benchmarks (utils/daily_stock_analysis)
